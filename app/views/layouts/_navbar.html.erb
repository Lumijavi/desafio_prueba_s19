<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
  <a class="navbar-brand" href="<%= root_path %>">Bucket list del 18°</a>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>

  <div class="collapse navbar-collapse" id="navbarSupportedContent">
    <ul class="navbar-nav mr-auto">
      <% if user_signed_in? %>
        <li class="nav-item">
          <a class="nav-link" href="#">Completados <%= CompletedTask.where(user_id: current_user.id, completed: true).count %> / <%= Task.all.count %><span class="sr-only">(current)</span></a>
        </li>
      <% end %>
    </ul>
    <ul class="navbar-nav ml-auto">
      <li class="nav-item active">
        <% if user_signed_in? %>
          <%= link_to "Salir", destroy_user_session_path, method: :delete, class: 'nav-link' %>
        <% else %>
          <a class="nav-link" href="<%= new_user_session_path %>">Iniciar Sesión<span class="sr-only">(current)</span></a>
        <% end %>
      </li>
    </ul>
  </div>
</nav>
